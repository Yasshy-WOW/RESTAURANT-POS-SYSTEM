# 簡易POSアプリ 操作方法

GitHubへの連携(プッシュ)前に、システムが一通り正しく動作することを手元で確認するための手順書です。
セットアップの詳細(依存パッケージのインストール等)は [README.md](./README.md) を参照してください。本書は「起動」と「動作確認の操作手順」に絞っています。

---

## 0. 前提条件(初回のみ)

以下が完了していることを確認してください。未実施の場合はREADME.mdの手順を先に行ってください。

- `backend/.env` が作成済み(`DATABASE_URL`・`JWT_SECRET_KEY`・`INITIAL_ADMIN_PASSWORD`等を設定済み)
- `backend`で `poetry install` 実行済み
- `frontend`で `npm install` 実行済み
- `poetry run alembic upgrade head` でテーブル作成済み
- `poetry run python scripts/seed_initial_admin.py` で初期管理者(`staff_id=0001`)投入済み

---

## 1. 起動方法

### 1-1. バックエンドを起動する

```bash
cd backend
poetry run uvicorn app.main:app --reload
```

起動後、ブラウザで以下にアクセスし、`{"status":"ok"}` が表示されればOKです。

- **URL: http://127.0.0.1:8000/health**

### 1-2. フロントエンドを起動する(別のターミナルを開く)

```bash
cd frontend
npm run dev
```

> **重要**: アクセスするURLは必ず `localhost` を使ってください。`127.0.0.1` でアクセスするとNext.jsの開発モードのセキュリティ機能でブロックされ、画面が正しく動きません。

- **URL: http://localhost:3000**

両方起動している状態で、次の動作確認に進みます。

---

## 2. 動作確認チェックリスト

上から順番に操作してください。要件仕様書・テスト仕様書の主要な結合テスト項目に対応しています。

### 2-1. ログイン

- **URL: http://localhost:3000/login**
- 担当者ID: `0001`
- パスワード: `backend/.env` の `INITIAL_ADMIN_PASSWORD` に設定した値
- [ ] ログインに成功し、自動的に `/pos` (POSメイン画面) に遷移する
- [ ] 誤ったパスワードでログインするとエラーが表示される(意図的に試す)

### 2-2. メニュー管理(管理者機能)

- **URL: http://localhost:3000/admin/menus**
- [ ] メニュー名・単価を入力して「登録」→ 一覧に4桁のメニュー番号付きで追加される
- [ ] 単価に `0` や `-100` を入力すると登録できずエラーになる
- [ ] メニュー名を51文字以上にすると登録できずエラーになる
- [ ] 「編集」→ 内容変更 → 「保存」で反映される
- [ ] 「削除」→ 確認ダイアログが出る → 「はい」で一覧から消える
- [ ] 「削除済みを表示」にチェック → 削除したメニューが再表示され「復元」ボタンが出る
- [ ] 「復元」→ 確認ダイアログなしで即座に一覧に戻る

### 2-3. 会員管理

- **URL: http://localhost:3000/admin/members**
- [ ] 氏名・電話番号・住所・性別・年齢を入力して登録 → 8桁の会員IDが自動採番される
- [ ] 編集・削除・削除済み表示・復元が2-2と同様に動作する

### 2-4. 担当者管理

- **URL: http://localhost:3000/admin/staff**
- [ ] パスワードと権限区分(一般担当者/管理者)を入力して登録 → 4桁の担当者IDが自動採番される
- [ ] 空のパスワードでは登録できずエラーになる
- [ ] 管理者が自分1人だけの状態で、その担当者(`0001`)を「削除」または「一般担当者に降格」しようとするとエラーになる(最後の管理者保護)

### 2-5. 消費税率管理

- **URL: http://localhost:3000/admin/tax-rate**
- [ ] 「現在の消費税率: 10%」と表示される
- [ ] `-1` や `101`、`8.5` のような小数を入力すると登録できずエラーになる
- [ ] `8` など0〜100の整数を入力すると変更でき、表示が更新される

### 2-6. POSメイン画面(レジ業務の一連の流れ)

- **URL: http://localhost:3000/pos**
- [ ] 「お客様ID読み込み(会員なし)」を押すと、会員なしのまま先に進める
- [ ] メニュー番号を手入力し「読み込み」→ 名称・単価が表示される
- [ ] 「購入リストへ追加」→ 購入リストに1行追加され、合計金額が表示される
- [ ] 同じメニューをもう一度追加すると、行が増えず数量が+1される
- [ ] 購入リストの行をクリックして選択 → 数量変更・削除ができる
- [ ] 購入リストが0件のとき「購入」ボタンが押せない(非活性)
- [ ] 1件でもあると「購入」ボタンが押せる
- [ ] 「購入」を押すと、税込み・税抜き合計を示すポップアップが出る
- [ ] 「閉じる」を押すと、購入リスト・会員ID表示がすべてクリアされ、最初の状態に戻る

### 2-7. 権限による画面制御

- 一度ログアウトし、2-4で作成した「一般担当者」でログインし直す
- [ ] 画面上部のナビゲーションに「担当者管理」等マスタメンテナンスへのリンクが表示されない
- [ ] ブラウザのアドレスバーに直接 `http://localhost:3000/admin/menus` 等を入力してもアクセスできない旨が表示される

### 2-8. ログアウト

- [ ] 右上の「ログアウト」ボタンを押すと、ログイン画面に戻る
- [ ] ログアウト後、ブラウザの「戻る」等でPOS画面に戻ろうとしてもログイン画面に遷移する

---

## 3. 停止方法

各ターミナルで `Ctrl + C` を押してください。

---

## 4. GitHubへ連携する前の最終確認

```bash
git status
```

を実行し、以下を確認してください。

- [ ] `backend/.env`(実際のパスワード・接続情報を含むファイル)が一覧に出てこない(`.gitignore`で除外済みのはず)
- [ ] `frontend/.env.local` が一覧に出てこない
- [ ] `import/`フォルダの中身(配布されたDBダンプ等)が一覧に出てこない
- [ ] 上記以外の、コミットして問題ないファイルのみが表示されている

問題なければコミット・プッシュに進んでください。

---

## 5. うまく動かないときは

| 症状 | 確認すること |
|---|---|
| ログイン画面が真っ白・操作しても反応しない | `http://127.0.0.1:3000` ではなく `http://localhost:3000` でアクセスしているか |
| ログインでエラーになる | パスワードが正しいか(`.env`の`INITIAL_ADMIN_PASSWORD`と一致しているか) |
| 画面全体で401エラーになる | バックエンド(`uvicorn`)が起動しているか、`http://127.0.0.1:8000/health`にアクセスできるか |
| Azure MySQLへの接続でタイムアウトする | Azure Portalの「ネットワーク」設定で、現在のPCのIPアドレスがファイアウォールに許可されているか(IPは変わることがあるため要再確認) |
| メニュー名や氏名が文字化けする | サーバーへの登録をcurl等で直接行った場合、文字コードがUTF-8になっているか確認(画面のフォームから登録する分には問題ありません) |
