# 簡易POSアプリ 要件仕様書（ドラフト v1.4）

対象範囲：Lv1「簡易POSアプリ 初級編」＋Lv2「簡易POSアプリ改」を統合した最終形

> 本書は「Lv1_簡易POSアプリ初級編_要求一覧.docx」「Lv2_簡易POSアプリ改_要求一覧.docx」に記載された**要求**をもとに、**要件**として整理したドラフトです。
> 実装方法（API設計・DB設計・Azureサービスの具体選定など）はあえて含めていません。
> 要求一覧だけでは判断できない項目は「9. 未確定事項・要確認事項」にまとめてあります。**この章の内容は、ご回答いただき次第、本文に反映します。**
>
> **【重要】用語の変更について**：本アプリはファミリーレストラン向けのため、要求一覧上の「商品」を本書では「メニュー」、「商品コード」を「メニュー番号」と読み替えています（詳細は3.3節）。これはLv1・Lv2要求一覧の記述内容を変更するものではなく、レストランの業態に合わせた用語の言い換えです。

---

## 1. システム概要

ファミリーレストラン等で利用する、Webブラウザで動作する簡易POS（レジ）システム。
レジ担当者がメニュー番号の入力・バーコードスキャンによりメニューを登録し、購入リストを作成した上で購入を確定する。購入結果はデータベースに永続化する。

---

## 2. 前提条件（変更不可）

| 項目 | 内容 |
|---|---|
| 形態 | Webアプリケーション（ブラウザからアクセス） |
| フロントエンド | Next.js |
| バックエンド | FastAPI |
| インフラ | Microsoft Azure |
| データベース | Azure Database for MySQL Flexible Server |
| バックエンドのホスティング先（Azure上のどのサービスを使うか） | 本要件書のスコープ外。非機能要件（3章）を踏まえて設計時に選定する |
| 利用デバイス | カメラ付きデバイス（ノートPC・スマホ・タブレット）を前提とし、カメラでのバーコード読み取りに対応する |

---

## 3. 機能要件

### 3.1 レジ担当者ログイン

- レジ業務開始前に、担当者は「担当者ID」と「パスワード」を入力してログインする。
- ログイン専用画面を用意し、認証成功時にPOSアプリ画面へ遷移する。
- 外部ID基盤（SSO等）は使用せず、本システム単独でID・パスワードを管理する。
- 担当者ID・パスワードが誤っている場合（存在しない担当者IDを含む）、ログインさせずエラーである旨を伝える。
- ログイン失敗の回数制限（アカウントロック）は設けない。何度でも再試行できる。
- パスワードの最低文字数などの複雑性要件は設けない（学習用アプリのため）。
- ログイン後の認証（JWTトークン）が期限切れになった場合、自動的にログイン画面に戻す。その時点で作成中だった購入リストの内容は失われる。
- ログインした担当者の情報は、その後の取引記録に紐づく。
- ログアウト操作を用意する。ログアウトすると、ログイン画面に戻る。

### 3.2 会員情報の読み込み

- 買い物開始前〜購入確定までの間に、会員カードの番号（会員ID）をシステムに入力（またはバーコードスキャン）できる。読み込み成功時、画面には会員IDのみを表示し、氏名などの個人情報は表示しない。
- 会員には、会員ID・氏名・電話番号・住所・性別・年齢が登録済みであることを前提とする。
- 会員証を持たない客は、会員IDを入力せずに「お客様ID読み込みボタン」を押すことで、会員なしの通常取引としてメニュー登録・購入に進める。
- 会員IDが未入力のまま購入された場合はエラーとせず、「会員なし」の正常な取引として扱う。
- 入力された会員IDに対応する会員が存在しない場合、購入を先に進めさせず、エラーである旨を伝える（「未入力」とは区別する）。
- 一度読み込んだ会員IDは訂正できない。誤って読み込んだ場合は、画面のリロード等により最初からやり直す。

### 3.3 メニュー登録

> 本アプリはファミリーレストラン向けのため、要求一覧上の「商品マスタ」を**メニューリスト**、「商品コード」を**メニュー番号**と読み替える（決定事項）。

- メニューリストを用意し、各メニュー項目に一意の**メニュー番号**（固定4桁の連番：0001〜9999）を付与する。
- 各メニュー項目の伝票／カードには、メニュー番号をバーコード化したものを用意する（レジ担当者がこれをスキャンして注文登録できるようにするため）。
- カメラでスキャンしたバーコードが「メニュー番号」か「会員ID」かは、読み取った数字の桁数で判別する（メニュー番号は4桁、会員IDは8桁のため、桁数が異なり自動的に区別できる）。

#### 3.3.1 メニュー番号の手入力

- メニュー番号入力欄に番号を入力し、検索（読み込み）操作を行う。
- 入力された番号に一致するメニューがメニューリストに存在する場合、名称・単価（税込み価格）を画面に表示する。
- 一致するメニューが存在しない場合、「メニューが未登録です」等のメッセージを表示する。
- 検索結果は、ユーザー操作により購入リストへ追加できる。
- 購入リストへの追加後、番号入力欄・名称表示・単価表示はクリアされ、次のメニューを続けて登録できる状態に戻る。

#### 3.3.2 バーコードスキャンによるメニュー登録

- カメラで（メニュー番号をバーコード化した伝票／カードの）バーコードを読み取り、メニュー番号として認識する。
- 認識成功時、「1件追加された」ことがユーザーにわかるフィードバックを表示する。
- スキャンは連続して行える（1回ごとに終了させず、続けて次のメニューを読み取れる）。
- 手入力による登録と、挙動（新規追加／数量加算の判定）の考え方を揃える。

#### 3.3.3 購入リストへの反映ルール（手入力・スキャン共通）

- 購入リストに存在しないメニューを登録した場合、新しい1行として追加する。
- 購入リストに既に存在するメニューを続けて登録した場合、新しい行を増やさず、既存行の数量を増やす（1メニュー1行を維持する）。
- ただし、数量を増やすことで上限（99個）を超える場合は、数量を加算せずエラーである旨をユーザーに伝える（上限は3.4.3節の数量変更ルールと共通）。

### 3.4 購入リストの操作

- 購入リストは、名称・数量・単価（税込み価格）・小計（税込み価格）が確認できる一覧表示とする。

#### 3.4.1 メニューの選択

- 購入リストの中から特定の1メニューを選択できる。
- 選択中のメニューは、他の行と見分けがつくよう強調表示する。
- 選択したメニューの情報（名称・単価・数量など）を画面上に表示する。

#### 3.4.2 メニューの削除

- 選択状態のメニューを購入リストから削除できる。

#### 3.4.3 数量の変更

- 選択状態のメニューの数量を後から変更できる。
- 数量の下限は1個（0個にはできない。0にする操作は「削除」で行う）。
- 数量の上限は99個。
- 数量変更時、そのメニューの小計および購入リスト全体の合計金額に即時反映する。

### 3.5 会員特典の割引・値引き（Lv2要求から除外）

> **【スコープ除外の決定】** Lv2要求一覧 2.5節「会員特典の割引・値引きの適用」は、今回の要件仕様から除外することが決定しました。値引き対象商品・適用期間・値引き種別（割合／金額）などの機能は実装しません。会員は「会員か否か」「会員情報の紐づけ」のみ扱い、金額面での特典は設けません。

### 3.6 購入確定

- 購入リストが0件（空）の場合、購入確定操作はできない（購入ボタンを非活性化するなど、確定できない状態にする）。
- 購入リストの中に、確定操作の時点でメニューリストから削除済みのメニューが含まれる場合、購入確定はできず、エラーである旨をユーザーに伝える。
- 購入リストの内容を確定すると、購入結果はデータベースに保存される。
- 保存内容には、少なくとも「いつ・何を・いくつ・いくらで・誰（会員）が買って・誰（担当者）がレジ処理をしたか」が後から判別できる情報を含める。
- メニューの単価は将来変更されうるが、購入確定済みの過去取引は、その後のメニューリストの単価変更の影響を受けず、購入当時の単価のまま記録が保持される。
- 消費税率についても同様に、購入確定済みの過去取引は、その後の消費税率変更の影響を受けず、購入当時（確定時点）の税率で計算された金額のまま記録が保持される。
- 購入確定後、税込み・税抜きの両方の合計金額をポップアップ等でユーザーに表示する。
- ポップアップを閉じる操作を行うと、コード入力・名称表示・単価表示・購入リストの内容、および会員IDがクリアされ、次の会員ID登録から再開できる状態に戻る。

### 3.7 消費税

- メニューリストに登録する単価は**税込み価格**とする。税抜き金額は、消費税率を用いてこの税込み価格から逆算する。
- 消費税額（税抜き金額）の計算は、**購入リストのメニュー行ごと**に行い、それらを合計する。端数（1円未満）は各行の計算時に**四捨五入**する。
- 購入確定時、税込み・税抜き両方の合計金額を表示する。
- 消費税率は、プログラムコードを書き換えずに変更できる状態（データとして保持）にする。
- 消費税率の初期値は**一律10%**とする（軽減税率は考慮しない）。

### 3.8 マスタメンテナンス機能

- 担当者マスタ・会員マスタ・メニューリスト・**消費税率**について、登録・編集（作成／更新／削除）ができる管理用の画面を用意する。
- 上記マスタの初期データは、このマスタメンテナンス画面を通じて登録する（DBへの直接投入は前提としない）。ただし、この画面自体が「管理者」権限を持つ担当者のみ利用可能であるため、**システム稼働開始時点で管理者が1人もいない状態から最初の管理者を作る手段に限っては、この原則の例外として、初期構築時にDBへ直接投入することを認める**（決定事項No.29）。2人目以降の担当者・会員・メニュー・消費税率は、本節の原則どおりマスタメンテナンス画面を通じて登録する。
- 本機能は、「管理者」権限を持つ担当者のみが利用できる。そのため、担当者マスタには権限区分（一般担当者／管理者）を持たせる。
- 一般担当者でログインした場合、マスタメンテナンス画面への導線は表示しない、またはアクセスをエラーとする。
- 会員ID・担当者ID・メニュー番号は、新規登録時にシステムが自動採番する（管理者が手入力する必要はない）。
- 管理者権限を持つ担当者が0人になることを防ぐため、最後の1人の管理者は削除・一般担当者への降格ができないようにする。
- 削除操作（担当者・会員・メニューいずれも）を行う前に、確認ダイアログ（「本当に削除しますか？」等）を表示する。

---

## 4. 画面要件（一覧）

要求一覧に基づき、最低限以下の画面が必要と考えられる。

| 画面 | 概要 |
|---|---|
| ログイン画面 | 担当者ID・パスワードを入力してログインする |
| POSメイン画面 | メニュー番号入力／読み込みボタン、名称・単価表示、購入リストへの追加ボタン、購入リスト（一覧・選択・削除・数量変更）、購入ボタン、ログイン中の担当者情報、会員ID表示・入力、バーコードスキャン機能、ログアウトボタン |
| 購入確定ポップアップ | 税込み・税抜き合計金額の表示、閉じる操作 |
| マスタメンテナンス画面 | 担当者・会員・メニュー・消費税率の登録／編集／削除 |

Lv1要求一覧に添付の画面イメージ（コード入力エリア／読み込みボタン／名称表示／単価表示／購入リストへ追加ボタン／購入リスト／購入ボタン）を土台とし、Lv2要求（会員ID・レジ担当者情報の表示、バーコードスキャン、選択・削除・数量変更UI）を追加したものになる想定。

---

## 5. データとして保持すべき情報（概念レベル）

具体的なテーブル設計は仕様書のスコープ外だが、要求から「情報として保持されるべきもの」を整理すると以下の通り。

- **メニューリスト**：メニュー番号（固定4桁：0001〜9999）、名称、単価（**税込み価格**。将来変更されうる）
- **会員マスタ**：会員ID（**数字のみ固定8桁**）、氏名、電話番号、住所、性別、年齢
- **担当者マスタ**：担当者ID（**数字のみ固定4桁**）、パスワード（認証情報。安全な方式で保管する）、権限区分（一般担当者／管理者）
- **消費税率**：初期値10%。プログラムから独立して変更可能なデータ
- **取引（購入）記録**：取引日時、購入担当者、購入会員（任意）、購入メニューの内訳（メニュー・数量・**購入当時の**単価）、**適用された消費税率**、税込み／税抜き合計金額

---

## 6. 非機能要件

- フロントエンド（Next.js）とバックエンド（FastAPI）で役割分担し、API連携により実現すること。
- 購入データ・単価変更履歴を含め、データは失われてはならない（購入確定時にDBへの永続化が必須）。
- 営業時間内は常に同程度の応答速度で動作すること。しばらくアクセスがなかった直後にだけ極端に遅くなる、という挙動は避けること（コールドスタートを避ける構成が望ましい）。
- バックエンドは、DBとの接続をリクエストごとに一から確立するのではなく、ある程度維持した状態で運用することが望ましい。
- **応答時間の目標**：メニュー検索・購入リストへの追加などの主要操作は、3秒以内にレスポンスを返すこと。
- **同時利用担当者数の目標**：1店舗あたり5〜10人が同時にレジ業務を行っても、上記の応答時間目標を維持できること。

---

## 7. スコープ外（本要件書では扱わない）

- API仕様・DBテーブル設計などの実装設計
- Azure上の具体的なホスティングサービスの選定
- メニュー番号バーコードを印字した伝票／カードの発行・運用方法の詳細（本書では「バーコード化した伝票／カードを用意する」という要件のみを扱う）

---

## 8. 用語集

| 用語 | 説明 |
|---|---|
| 担当者 | レジを操作するPOSシステムの利用者（従業員） |
| 会員 | 会員カード（会員ID）を持つ顧客 |
| メニュー | 注文可能な料理・商品の項目（要求一覧上の「商品」に相当） |
| メニュー番号 | 各メニューに付与される一意の番号（要求一覧上の「商品コード」に相当） |
| 購入リスト | 会計前に登録されたメニューの一覧（カートに相当） |

---

## 9. 決定事項（確認済み）

| No. | 項目 | 決定内容 |
|---|---|---|
| 1 | 商品コードの形式 | 「商品」を「メニュー」と読み替え、メニューリストを作成。各メニューに固定4桁（0001〜9999）のメニュー番号を付与する |
| 2 | バーコードスキャンとの関係 | メニュー番号をバーコード化した伝票／カードを用意し、それをスキャンする方式とする（Lv2のバーコード要件は維持） |
| 3 | 消費税率の初期値 | 一律10%（軽減税率は考慮しない） |
| 4 | 存在しない会員ID・担当者IDの扱い | 会員ID・担当者IDともに、存在しない場合はエラー表示とする（「未入力」とは区別する） |
| 5 | パスワードの保管方式 | 「安全な方式（ハッシュ化等）で保管すること」を要件として明記する |
| 6 | 会員特典の値引き・割引機能 | Lv2要求 2.5節をスコープから除外し、実装しない（3.5節参照） |
| 7 | 対応言語・通貨単位 | 日本語UI・日本円のみとする |
| 8 | 非機能要件の数値目標 | 主要操作の応答時間3秒以内／同時利用担当者数5〜10人（1店舗あたり）を目標とする |
| 9 | マスタデータの初期投入方法 | マスタメンテナンス画面（担当者・会員・メニューの登録／編集）をスコープに含める（3.8節） |
| 10 | マスタメンテナンス画面の利用権限 | 「管理者」権限を持つ担当者のみ利用可能とする。担当者マスタに権限区分（一般担当者／管理者）を持たせる |
| 11 | 消費税率の管理場所 | 消費税率もマスタメンテナンス画面で編集可能とする（3.8節） |
| 12 | 消費税率変更時の過去取引の扱い | 過去の取引記録は確定時点の税率で固定され、その後の税率変更の影響を受けない（メニュー単価と同様） |
| 13 | 会員ID・担当者IDの形式 | 会員IDは数字のみ固定8桁、担当者IDは数字のみ固定4桁とする |
| 14 | バーコード種別（メニュー／会員）の判別方法 | 読み取った数字の桁数で判別する（メニュー番号4桁／会員ID8桁） |
| 15 | 購入リストが空の場合の購入確定操作 | 購入ボタンを非活性化し、空のままの確定はできないようにする |
| 16 | バーコードスキャンで数量上限（99個）を超える場合 | 数量を加算せず、エラーである旨をユーザーに伝える |
| 17 | ログアウト機能 | ログアウトボタンを用意し、押すとログイン画面に戻る |
| 18 | 会員ID・担当者ID・メニュー番号の採番方法 | マスタメンテナンス画面での新規登録時、システムが自動採番する（管理者の手入力は不要） |
| 19 | 会員IDの訂正可否 | 一度読み込んだ会員IDは訂正不可。誤った場合は画面のリロード等で最初からやり直す |
| 20 | 会員情報の画面表示範囲 | 会員IDのみ表示し、氏名等の個人情報は画面に表示しない |
| 21 | ログイン失敗の回数制限 | 設けない（何度でも再試行可） |
| 22 | 管理者0人防止の制約 | 最後の1人の管理者は削除・一般担当者への降格ができないようにする |
| 23 | メニュー単価の税表示区分 | メニューリストに登録する単価は税込み価格とする。税抜き金額はここから逆算する |
| 24 | 消費税額の計算単位・端数処理 | 購入リストのメニュー行ごとに税抜き金額を計算し、それらを合計する。端数は各行で四捨五入する |
| 25 | 削除済みメニューを含む購入確定 | 確定時点でメニューリストから削除済みのメニューが購入リストに含まれる場合、確定不可としエラーを表示する |
| 26 | パスワードの複雑性要件 | 最低文字数などの要件は設けない（学習用アプリのため） |
| 27 | マスタメンテナンスの削除確認 | 削除操作の前に確認ダイアログを表示する |
| 28 | JWTトークン期限切れ時の挙動 | 自動的にログイン画面に戻す。作成中の購入リストは失われてよい |
| 29 | 最初の管理者アカウントの作成方法 | マスタメンテナンス画面経由での登録が原則（3.8節）だが、その画面を使うための最初の管理者自身は画面経由で作れない（鶏と卵の問題）。そのため、最初の1人の管理者に限り、システム初期構築時にDBへ直接投入することを例外的に認める。2人目以降の担当者はマスタメンテナンス画面から登録する |

## 10. 未確定事項・要確認事項

現時点で、要求一覧の内容から要件仕様書として決めておくべき論点はすべて解決しています（旧版で例示していた「管理者権限の初期登録方法」は、設計仕様書とのすり合わせにより決定事項No.29として解決済みです）。

これから設計・実装を進める中で新たな論点（例：権限のないユーザーがマスタメンテナンス画面にアクセスした際の具体的な画面遷移、など）が出てきた場合は、都度この章に追加していきます。

---

*本ドラフトはv1.4です。5周にわたるレビューおよび設計仕様書とのすり合わせにより、当初存在した曖昧な点はすべて確認・反映済みです。今後、設計・実装を進める中で新たな論点が出てきた場合は、都度この文書に追加していきます。*

---

## 11. 変更履歴

| バージョン | 主な変更内容 |
|---|---|
| v0.1 | 初版ドラフトを作成（Lv1＋Lv2統合）。要求一覧の内容を要件として整理し、要求だけでは判断できない8項目を「未確定事項」として提示 |
| v0.2 | 要求一覧上の「商品」を「メニュー」、「商品コード」を「メニュー番号」と読み替えることを決定（3.3節）。バーコード方式（メニュー番号をバーコード化した伝票をスキャン）を決定。消費税率初期値を一律10%に決定。存在しない会員ID・担当者IDはエラー表示とすることを決定 |
| v0.3 | パスワードを安全な方式で保管する要件を明記。会員特典の値引き・割引機能をLv2要求からスコープ除外することを決定（3.5節）。対応言語・通貨単位を日本語・日本円のみに決定 |
| v0.4 | 非機能要件に数値目標（応答時間3秒以内、同時利用担当者数5〜10人）を追加。マスタメンテナンス機能を新設（3.8節）し、画面要件に追加 |
| v0.5 | マスタメンテナンス画面の利用権限を「管理者」権限保有者のみに限定することを決定。担当者マスタに権限区分を追加 |
| v0.6 | マスタメンテナンス画面の対象に消費税率を追加（API概要の検討過程で判明した抜け漏れを反映） |
| v0.7 | 消費税率を変更した場合も、過去の取引記録は確定時点の税率のまま固定されることを決定（3.6節・5節に反映） |
| v0.8 | 会員ID（数字のみ固定8桁）・担当者ID（数字のみ固定4桁）の形式を決定（DB設計の検討過程で判明した抜け漏れを反映） |
| v0.9 | 【レビュー1周目】用語統一の誤記（「商品登録」の残存）を修正。バーコードで読み取った番号が「メニュー番号」か「会員ID」かを桁数で判別する方式を決定。購入リストが空の場合は購入確定不可とすることを決定。バーコードスキャンで数量が上限（99個）を超える場合はエラーとすることを決定 |
| v1.0 | 【レビュー2周目】ログアウト機能を追加（3.1節・4節）。会員ID・担当者ID・メニュー番号はマスタメンテナンス画面でシステムが自動採番する方式に決定。一度読み込んだ会員IDは訂正不可とすることを決定 |
| v1.1 | 【レビュー3周目】会員情報は会員IDのみ画面表示し、氏名等は表示しないことを決定。ログイン失敗の回数制限は設けないことを決定。管理者権限保有者が0人にならないよう、最後の1人は削除・降格不可とする制約を追加 |
| v1.2 | 【レビュー4周目】メニューリストの単価は税込み価格として登録する方針を決定（3.7節・5節に反映、単価表示箇所の記述も修正）。消費税額はメニュー行ごとに計算し四捨五入して合計する方式を決定。確定時点で削除済みのメニューが購入リストに含まれる場合はエラーとすることを決定 |
| v1.3 | 【レビュー5周目】パスワードの複雑性要件は設けないことを決定。マスタメンテナンス画面での削除操作前に確認ダイアログを表示することを決定。JWTトークン期限切れ時は自動的にログイン画面に戻り、作成中の購入リストは失われる仕様に決定 |
| v1.4 | 【設計仕様書とのすり合わせ】設計仕様書の作成・レビュー過程で判明した、3.8節「マスタの初期データはマスタメンテナンス画面を通じて登録する（DB直接投入は前提としない）」と「その画面は管理者しか使えない」という規定同士の矛盾（最初の管理者をどう作るか＝旧版9章で未確定事項として名指ししていた論点）を解消。最初の1人の管理者アカウントに限り、システム初期構築時にDBへ直接投入することを例外的に認めることを決定（3.8節・決定事項No.29）。10章の記述を更新 |
